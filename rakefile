require 'erb'

generated = ["js/fast.js"];

task :default => generated;
file "js/fast.js" => FileList["src/fast*.erb.js", "src/*.rb"] do |t|
	puts "generating fast"
	sh "erb src/fast.erb.js > #{t.name}" 
end

task :clean do
	rm generated
end
	

